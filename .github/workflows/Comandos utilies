    Validar cada modulo
for module in modules/*; do if [ -d "$module" ]; then echo "Validando: $module"; cd "$module"; terraform init -backend=false -input=false > /dev/null 2>&1 && terraform validate; cd - > /dev/null; fi; done

    