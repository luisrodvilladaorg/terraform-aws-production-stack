name: Terraform CD

on:
    workflow_dispatch:
        inputs:
            environment:
                description: "Environment to deploy"
                required: true
                default: "dev"

jobs:
    terraform-apply:
        name: Terraform Apply
        runs-on: self-hosted

        defaults:
            run:
                shell: bash

        steps:
            # 1️⃣ Checkout repository
            - name: Checkout repository
              uses: actions/checkout@v4

            # 2️⃣ Setup Terraform
            - name: Setup Terraform
              uses: hashicorp/setup-terraform@v3
              with:
                  terraform_version: 1.6.6

            # 3️⃣ Verify AWS identity (basic security check)
            - name: Verify AWS identity
              run: aws sts get-caller-identity

            # 4️⃣ Terraform Init
            - name: Terraform Init
              working-directory: envs/${{ inputs.environment }}
              run: terraform init -input=false

            # 4.0️⃣ Validate environment input
            - name: Validate environment input
              run: |
                if [ "${{ inputs.environment }}" != "dev" ] && [ "${{ inputs.environment }}" != "prod" ]; then
                  echo "Invalid environment: ${{ inputs.environment }}. Allowed: dev, prod"
                  exit 1
                fi

            # 4.1️⃣ Ensure required secret exists
            - name: Check required secrets
              env:
                TF_VAR_db_password: ${{ secrets.TF_VAR_db_password }}
              run: |
                if [ -z "$TF_VAR_db_password" ]; then
                  echo "Missing required secret: TF_VAR_db_password"
                  exit 1
                fi

            # 5️⃣ Terraform Plan (preview)
            - name: Terraform Plan
              working-directory: envs/${{ inputs.environment }}
              env:
                TF_VAR_db_password: ${{ secrets.TF_VAR_db_password }}
              run: terraform plan -input=false

            # 6️⃣ Terraform Apply (automatic, confirmed via workflow_dispatch)
            - name: Terraform Apply
              working-directory: envs/${{ inputs.environment }}
              env:
                TF_VAR_db_password: ${{ secrets.TF_VAR_db_password }}
              run: terraform apply -auto-approve
